This update intentionally does NOT include foodhouse_pos.db.
Keep your existing local database file if you use SQLite.
On Render/PostgreSQL, the app creates the new engagement tables and additive columns automatically at startup.
