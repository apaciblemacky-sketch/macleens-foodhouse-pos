# CHAT Lite Lifetime + Admin Free Access

Extract this ZIP into the existing Macleen's project root and replace matching files.

This incremental update assumes the previous Macleen's affected-files update is already installed.

Changes in this package:
- Keeps CHAT Lite ₱5 per-use access.
- Adds optional one-time Lifetime Access with an editable price in Digital Admin.
- Adds free unlimited Admin access at `/admin/chat-lite`, protected by the existing Admin login.
- Adds an “Open CHAT Lite Free” button in Digital Admin.
- Admin/Lifetime hosts create public guest join links without exposing admin or paid-order URLs.
- Per-use passes remain resumable until the 6-hour expiry after refresh, browser crash, internet loss, or PC shutdown.
- Only intentionally using Leave ends a per-use pass early.
- CHAT Lite remains ephemeral: messages, files, and calls are not restored after a room ends.
